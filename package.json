{
  "name": "discord-user-connector",
  "version": "0.2.0",
  "description": "MCP server that lets AI agents read Discord data using a user token, emulating a normal Discord client",
  "type": "module",
  "module": "src/index.ts",
  "bin": {
    "discord-user-connector": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "check": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "bun-types": "^1.4.2"
  }
}
